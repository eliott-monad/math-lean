/-
# Path homology of tournaments

Provenance: `eliottcassidy2000/math` — path homology computations on tournaments
(Grigor'yan–Lin–Muranov–Yau path homology of digraphs).

Target results (stubs — see `candidates/`):
- The path chain complex of a tournament and its homology groups.
- The informal computations of Betti numbers for small tournaments.

Kept `sorry`-free: commit a definition/result only once it compiles.
-/
import Math.Tournaments.Basic

namespace Math.PathHomology

-- (formalizations land here)

end Math.PathHomology
